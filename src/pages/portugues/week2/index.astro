---
import type { isRenderTemplateResult } from 'astro/dist/runtime/server/render/astro'
import BaseLayout from '../../../layouts/BaseLayout.astro'
import Array from '../../../components/array.astro'
import { getEntry } from 'astro:content'

const verbosIrregulares = await getEntry('portugues', 'verbos-irregulares')
const data = [
  {
    title: 'Presente',
    arrayName: 'presente',
  },
  {
    title: 'Presente do Subjuntivo',
    arrayName: 'presente-do-subjuntivo',
  },
  {
    title: 'Preterito Perfeito',
    arrayName: 'preterito-perfeito',
  },
  {
    title: 'Preterito Imperfeito',
    arrayName: 'preterito-imperfeito',
  },
  {
    title: 'Futuro do Subjuntivo',
    arrayName: 'futuro-do-subjuntivo',
  },
  {
    title: 'Ejemplo',
    arrayName: 'ejemplos',
  },
]
---

<BaseLayout title="Portugues">
  <div class="wrapper text-sm sm:px-12 sm:text-lg">
    <div class="one bg-base-200 p-2">Semana 2</div>
    <div class="two bg-base-300 px-4 py-2">
      <div class="font-bold">Explicacion de un projecto que hice:</div>
      <div>
        Recentemente, concluí um projeto envolvendo a criação de uma loja de
        café utilizando as tecnologias React e Node JS. Neste projeto,
        desenvolvi uma plataforma que permite aos usuários explorar diferentes
        tipos de café, fazer pedidos e até mesmo personalizar suas bebidas de
        acordo com suas preferências. Utilizei React para criar uma interface de
        usuário interativa e responsiva, enquanto o Node JS foi empregado para
        construir o backend que gerencia os pedidos, estoque e interações com os
        clientes. Estou muito satisfeito com o resultado alcançado e com as
        habilidades que adquiri ao longo desse processo
      </div>
    </div>
    <div class="three bg-base-200 px-4 py-2">
      <div class="font-bold">Temas:</div>
      <div>* Mas e Mais.</div>
      <div>* Verbos irregulares</div>
      <div>* Comenzar a preparar un video sobre el trabajo remoto.</div>
    </div>
    <div class="four bg-base-200 p-2">Proximamente.</div>
    <div class="five bg-base-300 p-2">Proximamente.</div>
    <div class="six bg-base-200 p-2">Proximamente.</div>
    <div class="seven bg-base-300 p-2">Proximamente.</div>
    <div class="eight bg-base-200 px-4 pb-4">
      <h1 class="mb-2">Verbos Irregulares</h1>
      {
        verbosIrregulares.data.map((item, index) => {
          return (
            <>
              <div>
                <div>
                  <div class="font-bold mt-4">
                    {index + 1}: {item.title}
                  </div>
                </div>
                <Array title={data[0].title} arreglo={item[data[0].arrayName]} />                
                <Array title={data[1].title} arreglo={item[data[1].arrayName]} />                
                <Array title={data[2].title} arreglo={item[data[2].arrayName]} />                
                <Array title={data[3].title} arreglo={item[data[3].arrayName]} />                
                <Array title={data[4].title} arreglo={item[data[4].arrayName]} />                
                <Array title={data[5].title} arreglo={item[data[5].arrayName]} />                
              </div>
            </>
          )
        })
      }
    </div>
  </div>
</BaseLayout>

<style>
  .wrapper {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-gap: 10px;
    grid-auto-rows: minmax(100px, auto);
  }
  .one {
    grid-column: 1 / 2;
    grid-row: 1;
  }
  .two {
    grid-column: 2 / 4;
    grid-row: 1 / 3;
  }
  .three {
    grid-column: 1;
    grid-row: 2 / 5;
  }
  .four {
    grid-column: 3;
    grid-row: 3;
  }
  .five {
    grid-column: 2;
    grid-row: 4;
  }
  .six {
    grid-column: 3;
    grid-row: 4;
  }
  .seven {
    grid-column: 2;
    grid-row: 3;
  }
  .eight {
    grid-column: 1/4;
    grid-row: 5;
  }
</style>
